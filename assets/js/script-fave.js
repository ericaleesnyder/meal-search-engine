//RAGHAD - write the JS to create cards from the array

var favoritedRecipes = [];
var cardDisplay = document.getElementById("card-tabs");

// let storeFav = JSON.parse(localStorage.getItem("storfav")) || []; 
console.log(storeFav)
var storeFav = []
function displayFavCards () {
  //     localStorage.setItem('fav food', JSON.stringify(favoritedRecipes));
  // for(let i =0; i storeFav.length; i++){
  //     let li = document.createElement("li")
  //     li.textContent = storeFav[i]
  //     FileList.appendchild(li)
  // favoritedBtn.setAttribute('data-index', i);
  // favoritedBtn.setAttribute('class', 'tabs tabs-fixed-width');
}

displayFavCards();

// when page load
// check local storage for the storeFav

// get values from the user input

// if value are valid

// display it in the screen

// var userInput = document.JSON
